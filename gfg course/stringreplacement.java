public class stringreplacement {
    public static String repl(String word,String sub){
        char[] ch = word.toCharArray();
        int n=ch.length;
        char[] su = sub.toCharArray();
        int n2=su.length;
        int index = word.indexOf(sub);
        int temp=su[0];
        int temp2=su[1];
        for(int i=0;i<n;i++){
            if(ch[i]=='?' || ch[i]==su[0]){
                if(ch[i+1]=='?' || ch[i+1]==su[1]){
                    string.append
                }
            }
        }
    }
}
