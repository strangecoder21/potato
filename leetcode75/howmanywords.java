public class howmanywords {
    public int many(String s){
        int count =1;
        char arr[]= new char[s.length()];
        for(int i=0;i<s.length();i++){
            arr[i]=s.charAt(i);
        }
        for(int i=0;i<arr.length;i++){
            if(arr[i]==" "||)
        }
    }
}
